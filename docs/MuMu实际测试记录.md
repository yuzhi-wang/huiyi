# MuMu 实际测试记录

测试日期：2026-09-13，约 21:38–21:46（北京时间）。

## 环境与范围

- 应用：会议记录，包名 `com.huiyi.meetingdemo`，交付版本 0.1.0 / 100。
- 安装包：`apk/huiyi-0.1.0.apk`；SHA-256：`93033BE59CE327DCD8CA5074C34AECF4A3CEACD62B6C45A8EAD847E121F85D4A`。
- 用户已在本机 MuMu 安装并启动，开发侧通过窗口实际操作 Android App；不是浏览器预览或模拟接口测试。
- 全程未填写 API Key / Workspace ID，未点击生成，未发起模型测试。
- 结论依据为界面与交互观察；没有采集进程证据或试听输出音频。

## 已执行结果

| 检查 | 实际观察 | 结果 |
| --- | --- | --- |
| 启动与历史 | 首页正常；原有 1 条 3 秒的“未命名会议”保留 | 通过 |
| 无配置录音 | 未配置模型时开始录音，计时增长，手动停止 | 通过 |
| 保存与列表 | 提示“录音已保存”；新增 `t`，历史由 1 条增至 2 条，状态“待生成” | 通过 |
| 详情信息 | `t`，9 月 13 日 21:38，00:31，0.10 MB | 通过（界面值） |
| 播放 | 点击后变为“暂停”，播放时间和进度条前进 | 通过（播放控制） |
| 暂停 | 播放中暂停后按钮变为“播放”，停在约 25 秒 | 通过 |
| 调整进度与继续 | 暂停时向前拖回，稳定后显示约 8 秒；继续后前进至 25 秒 | 通过 |
| 自然结束 | 播放到结尾后按钮恢复“播放”，进度复位 00:00 | 通过 |
| 关闭后重新打开 | 关闭 MuMu 顶部的应用标签，回到模拟器桌面，再点“会议记录”；首页仍有两条历史 | 通过（此关闭/重开路径） |
| 重开后的文件读取 | 再进 `t` 详情，仍为 31 秒、0.10 MB；重新播放进度前进至 14 秒 | 通过 |

关闭/重开测试没有核验 Android 进程是否被杀死，也未执行模拟器重启、系统重启或强制停止。不能将本次结果扩展为全部崩溃恢复场景已验证。

## 留给用户的记录

App 中保留了 **`t`：9 月 13 日 21:38，31 秒，0.10 MB，待生成**，原有 3 秒录音也保留。测试结束返回首页。

`t` 是现场麦克风录音，没有注入预设会议语音，也未确认其是否有清晰人声。请先试听；如果只有静音或环境声，请另录一段明确讲话的音频，再验证纪要准确性。名称 `t` 来自模拟器文字输入兼容限制，不是预设会议内容。

## 用户后续填写

Key 只在 App 内填写，不要写进本记录。每项可填写“通过 / 失败 / 未测”及现象。

| 项目 | 结果 | 现象或错误提示 |
| --- | --- | --- |
| 短录音人声清晰、开头结尾完整 | 待填写 | |
| 填写 Key / Workspace ID 后手动生成 | 待填写 | |
| 生成期间文字逐步出现 | 待填写 | |
| 摘要、关键事项、待办正确 | 待填写 | |
| 金额、数量、交期及最终结论准确 | 待填写 | |
| 无明确负责人/日期时显示“未明确” | 待填写 | |
| 纪要完成后退出重开仍保留 | 待填写 | |
| 断网/失败提示、手动重试、音频保留 | 待填写 | |
| 约 30 分钟录音、自动停止与完整回放 | 待填写 | |

可选朗读测试材料（此段尚未录入 `t`）：

> 今天讨论样品订单。客户询问能否采购一千件，每件二十八元，这只是询价，尚未确认订单。最终确认先寄十件样品，样品费共三百元，运费另算，运费金额待确认。张明负责在二〇二六年九月十五日前发出样品。李华提出批量订单交期二十天，但客户尚未确认，不能算作承诺。产品目录需要更新，负责人和截止日期还没有明确。

核对点：1000 件 / 28 元只是询价；确认的是 10 件样品、样品费 300 元；运费未定；张明的样品任务截止 2026-09-15；批量 20 天交期未确认；目录更新的负责人和日期应为“未明确”。

## 未执行及限制

- 未试听核验人声、内容、音质；界面时长与大小未用独立音频工具复核。
- 按用户要求未进行任何真实模型调用；原生流式传输、模型格式支持和纪要准确性仍待实际调用。
- 未执行长录音、权限拒绝、极短录音、后台/系统中断、存储不足、超限文件及覆盖安装的设备测试。
- 未执行 Android 真机测试；本次 MuMu 基础流程通过不代表所有设备或架构兼容性通过。
## 用户反馈：播放无声（待定位）

2026-09-13，用户反馈“播放我没有听到声音”。这表示可听见的录音/回放链路尚未通过，不能以进度条前进判定声音正常。上表的播放、暂停等通过仅指界面控制。

当前未明确是测试录音 `t`，还是用户主动讲话的新录音也无声。`t` 没有注入预设语音，录制时是否收到人声未确认。已检查业务源码：音源设置为 MIC，播放器未主动设为零音量；这些检查不能排除录入静音、系统/模拟器输入输出配置或实际原生播放问题。需用明确讲话的录音和可听见的播放结果继续定位，暂不修改代码或重新打包。