# 开发检查记录

日期：2026-09-13。全部功能一次实现，不设置分阶段用户验收关卡。

## 环境及交付状态

| 项目 | 实际结果 |
| --- | --- |
| 源码 | uni-app + Vue 3 + JavaScript，完整功能已实现 |
| App | 会议记录，`com.huiyi.meetingdemo`，0.1.0 / 100 |
| DCloud AppID | `__UNI__0BBF9A7`，官方 CLI 创建 |
| HBuilderX | 5.24.2026081301，已登录 |
| 独立编译器 | DCloud `3.0.0-5020420260813003`，Vue 3.4.21，Vite 5.2.8 |
| 本机 Node / npm | 26.1.0 / 11.13.0 |
| 固定签名 | 已生成 RSA 2048 位 JKS，公开指纹见使用说明 |
| APK | `apk/huiyi-0.1.0.apk`，20,293,134 字节，19:15 云打包成功 |
| APK SHA-256 | `93033BE59CE327DCD8CA5074C34AECF4A3CEACD62B6C45A8EAD847E121F85D4A` |
| MuMu | 已执行短录音保存、播放控制、关闭应用标签后重开保留；详见 [实际测试记录](MuMu实际测试记录.md) |
| Android 真机 | 未执行 |
| 阿里云真实调用 | 未执行，没有使用真实 Key |

## 已执行检查

`npm test`：**18/18 通过**。

| 范围 | 覆盖内容 |
| --- | --- |
| AAC 时长 | 完整帧计算、截断尾部识别、无效头拒绝；重复目录批次、空文件及临时录音恢复 |
| 大小边界 | 空文件、非整数、Base64 9,999,996 可接受 / 10,000,000 拒绝，实际长度、JSON UTF-8 字节限制 |
| SSE | 所有切分位置、逐字符、中文、CRLF、注释、usage、多行 data、累计 responseText 去重 |
| 结束判定 | 缺 stop / DONE / 事件终止空行、截断 JSON、length 结束、结束后继续输出均失败；等待 HTTP 完成 |
| 结果结构 | 摘要/事项/待办、空待办、负责人和日期 null、JSON 围栏、缺字段拒绝 |
| 持久化 | 重启生成中→失败、存储满时内存也标记中断、坏存储不覆盖、保存失败不能显示成功 |
| 单请求及重试 | 重复生成拒绝、预检失败不发请求、解析/存储失败保留音频和旧结果 |
| 配置和错误 | Workspace 不能替换域名、Key 拒绝换行、HTTP 错误不回显原始响应或凭据 |
| 录音控制（模拟原生） | 重复开始/停止只保存一次、释放资源、权限拒绝、准备中进后台、原生 stop 失败不假报成功 |

`npm run build:app`、`npm run build:h5` 均成功。`npm ci --dry-run --offline` 验证锁文件一致。App 输出为资源目录，不含可安装 APK。

浏览器 H5 390×844 预览已检查：首页空态、设置、空 Key 校验、占位配置保存、刷新清空配置、示例历史、生成中重启变失败、纪要详情、未明确字段、音频缺失错误。示例记录只在本地预览中，未打入 App。**这些结果不代表 Android 原生录音或真实模型通过。**

业务代码不输出原生/模型原始错误，不记录请求体、Key 或 Base64；配置不写入本地存储。源码和最终编译业务资源进行了凭据模式检查。签名密码仅在被忽略的 `.local` 目录。

## 已解除的外部阻碍（历史）

- 18:33 再次执行官方 `publish app-android --type appResource`，明确返回：**“当前账号尚未绑定手机号……绑定手机号后再重新打包”**。需本人在 `https://dev.dcloud.net.cn/pages/user/info` 完成绑定，无需向开发侧提供短信验证码。
- `pack --config` 返回“正在下载依赖插件，请稍后重试”；`installPlugin --name uniapp-cli-vite` 返回“安装失败”，没有提供具体下载错误；`pack status` 返回“暂无云打包状态”。
- 手机号已由用户绑定；HBuilderX 已迁至用户可写目录。19:09–19:12 官方 uniapp-cli-vite、amazon-corretto、launcher、app-safe-pack 全部安装成功。19:12 完整工程通过 HBuilderX 编译并提交云端。

## 最终 APK 实物检查

- 云打包类型：Android 自有证书，完整安装包，非自定义调试基座。
- `apksigner verify --verbose --print-certs` 通过；v1、v2 签名均有效，单个 RSA 2048 位签名者。证书 SHA-256 与本机固定证书完全一致：`c5996bc613f2833fdda21cb2a0e95a27c2211410bc95a786b8df7573d27dc775`。
- 解码实际 APK 后确认：应用名“会议记录”、包名 `com.huiyi.meetingdemo`、版本 0.1.0 / 100、minSdk 23、targetSdk 35；非 debuggable。
- APK 业务资源含 Android MediaRecorder、Qwen 模型调用、原生 XHR 流式处理及首页/详情页；未发现测试示例数据、签名密码文件、`.local` 目录或 Key 模式。
- 实际原生架构为 arm64-v8a、armeabi-v7a；云端没有输出配置中额外列出的 x86_64。当前 MuMu 已实际运行短录音、保存和播放控制流程；不能扩展为所有架构/设备兼容性结论。
- SDK 默认清单还声明了手机状态、媒体/外部存储及安装包等权限；业务未实现这些功能。本次未做应用商店隐私/权限审核，也未验证运行时是否出现额外权限提示。
- 签名工具另提示部分 `META-INF` 库元数据不受 v1 条目签名保护；整体 v2 签名验证通过，未改动已签名 APK。

## 用户安装后的待测项

完整 APK 已提供，可在 MuMu 一次测试全部功能，发现问题直接修复。

- 短录音保存及播放控制已在 MuMu 检查；约 30 分钟录音、独立时长/大小复核及开头/中间/结尾音质待测。
- 拒绝权限、极短录音、系统中断、后台停止、进程被杀/系统重启及存储不足；关闭应用标签后重开已检查。
- 真实 Key / Workspace 配置后先短后长调用，确认原生 XHR 增量、SSE 结束协议及格式支持。
- 流式展示、纪要保存、金额/数量/交期准确性、断网与手动重试。
- MuMu 安装由用户完成，启动及基础流程已实际检查；同签名覆盖安装和其他设备兼容性待测。包名、版本、签名、文件 SHA-256 已由开发侧检查。

尚无 Android 真机兼容性结论；MuMu 也不能代替真机验证。
