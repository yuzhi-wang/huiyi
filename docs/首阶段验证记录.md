# 早期验证用例（历史）

> 2026-09-13 已取消分阶段交付与验证。以下仅保留早期用例作为背景；当前检查与阻碍统一见《开发检查记录.md》，不要求用户先验收录音版后继续开发。

创建日期：2026-09-11。当前仅完成开发准备，下表实际 App 测试全部未执行。

执行步骤与判定依据见《开发准备与技术核查.md》第 6 节。最终验收还需独立安装 APK、Android 真机与真实模型调用。

## 环境

| 项目 | 待填写值 |
| --- | --- |
| 测试时间、执行人 | 未执行 |
| 源码提交、App 版本、包名 | 未创建 App |
| HBuilderX / Vue 3 编译器 / 基座版本 | 待 IDE 核实 |
| 手机型号 / Android 版本 / 内存 | 未连接验证 |
| APK 路径 / SHA-256 / 签名标识 | 未打包；不记录私钥或密码 |
| 测试网络 | 未执行 |
| 模型 / 地域 | 计划 qwen3.5-omni-plus / 北京 |
| Endpoint 模式 / Workspace 配置是否正确 | 待验证；不记录 API Key |

## 用例执行

| ID | 用例 | 必须保留的证据 | 结果 |
| --- | --- | --- | --- |
| E01 | 真机启动与麦克风授权/拒绝 | 工具、设备版本及权限状态 | 未执行 |
| R01 | 10 秒、1 分钟 AAC | 请求参数、实际格式/大小/时长、回放观察 | 未执行 |
| R02 | 重复开始/停止、立即停止、录音错误 | 状态变化、无重复保存或假成功 | 未执行 |
| R03 | 24kbps 候选连续约 30 分钟 | 开头/中段/结尾标记、实际时长、文件大小、音质 | 未执行 |
| R04 | 必要时 32kbps 候选对比 | 音质提升、实际大小与余量 | 未执行 |
| F01 | 离线保存、退出重开、回放 | 持久路径、文件存在、回放完整 | 未执行 |
| F02 | 空文件、文件缺失、保存失败 | 明确提示，保留可恢复音频 | 未执行 |
| B01 | 长文件 Base64 与请求构造 | 实际字符数、UTF-8 请求字节数、耗时与内存 | 未执行 |
| B02 | 大小边界及超限拒绝 | 边界数据、无请求、音频可回放 | 未执行 |
| S01 | 离线 SSE 分片/换行/usage | 输入夹具和自动检查结果 | 未执行 |
| S02 | 缺终止标记、length、断流、非法 JSON | 状态不成功、重试入口 | 未执行 |
| S03 | 真机 POST 增量传输 | 各增量到达时刻、首次显示早于连接完成 | 未执行 |
| A01 | 短 AAC 真实模型调用 | 脱敏请求 ID、模型、HTTP 状态、完成事件、usage | 未执行；本次无调用 |
| A02 | 30 分钟整段真实模型调用 | 大小预检、完成事件、可读纪要、人工事实核对 | 未执行；本次无调用 |
| A03 | 无明确待办、未明确负责人/日期、结论变更 | 对照录音结果，无猜测承诺 | 未执行 |
| A04 | 网络/鉴权/权限/限流/超时与手动重试 | 明确原因、一个请求、音频保留 | 未执行 |
| P01 | Key 清除/重启后的历史访问 | Key 不持久化，音频和完成结果仍可看 | 未执行 |
| P02 | 流中断后重启、结果落盘失败 | 不伪成功、不自动重发、旧结果不丢失 | 未执行 |
| K01 | 独立 APK 安装后重复主流程 | APK 哈希、真机操作和真实模型证据 | 未执行 |

## 长录音测量

| 指标 | 24kbps 候选 | 32kbps 候选（按需） |
| --- | --- | --- |
| 请求编码/容器/声道/采样率 | 待测 | 待测 |
| 实际编码/容器/声道/采样率 | 待测 | 待测 |
| 墙钟时长 / 媒体实际时长（ms） | 待测 | 待测 |
| 原文件大小 N（B） | 待测 | 待测 |
| 实测平均文件码率 8N/时长（含封装） | 待测 | 待测 |
| 理论 Base64 长度 / 实际长度 | 待测 | 待测 |
| JSON 请求体 UTF-8 字节数 | 待测 | 待测 |
| 读取/编码/序列化耗时 | 待测 | 待测 |
| 内存测量方法 / 基线 / 峰值 / 释放后 | 待测 | 待测 |
| 开头/中段/结尾回放、数字与中文清晰度 | 待测 | 待测 |

## 问题与阶段结论

- 已知文档限制：uni recorder 时长与码率、uni.request 分块支持，详见准备文档。
- 实测问题：尚无测试，不能填“未发现问题”。
- 阶段判定：未执行，未通过。
- 下一步：补齐 HBuilderX App 工具和真机连接，创建最小工程，先执行 E01 → R01 → F01，再做 R03 / B01。

只记录必要元数据与脱敏错误，不保存 Key、Authorization、完整请求体或 Base64 到日志/仓库。测试音频留在手机；需要另存证据时先明确其内容和用途。
